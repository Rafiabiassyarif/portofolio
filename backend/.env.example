# Database MySQL URL
# Format: mysql://<username>:<password>@<host>:<port>/<database_name>
DATABASE_URL="mysql://root:@localhost:3306/portfolio_db"

# JWT Secret untuk otentikasi admin (Ganti dengan string acak panjang di production)
# Generate contoh di terminal: openssl rand -base64 32
JWT_SECRET="ganti_dengan_jwt_secret_yang_sangat_rahasia_dan_panjang_minimal_32_karakter"

# Port backend (Pilih port bebas selain 5000 jika 5000 sudah terpakai di server)
PORT=5001

# CORS Origin (pisahkan dengan koma jika multiple origins, atau '*' jika single domain/reverse proxy)
CORS_ORIGIN="*"

# (Opsional) Setup Key untuk melindungi pemanggilan POST /api/admin/setup
# ADMIN_SETUP_SECRET="kunci_rahasia_setup_awal"

# (Opsional) Username & password admin inisial saat seed atau setup pertama
# ADMIN_INIT_USERNAME="admin"
# ADMIN_INIT_PASSWORD="password_admin_yang_sangat_kuat"
